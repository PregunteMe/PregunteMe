<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Control de calidad{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        
		<link href="//code.ionicframework.com/ionicons/1.5.2/css/ionicons.min.css" rel="stylesheet" type="text/css" />
		<meta content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no' name='viewport'>
        <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" rel="stylesheet" type="text/css" />
        <link href="//cdnjs.cloudflare.com/ajax/libs/font-awesome/4.1.0/css/font-awesome.min.css" rel="stylesheet" type="text/css" />
		<link rel='stylesheet' type='text/css' href="{{ asset('bundles/preguntemepresentacion/css/AdminLTE.css') }}" rel="stylesheet" type="text/css" />    
		<link rel='stylesheet' type='text/css' href="{{ asset('bundles/preguntemepresentacion/css/estilos.css') }}" />
		
		<!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
        <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
        <!--[if lt IE 9]>
          <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
          <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
        <![endif]-->
        
        <script src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
		
    </head>
<body class="skin-blue">
        <!-- header logo: style can be found in header.less -->
        <header class="header">
            <a href="{{path('index')}}" class="logo">
                <!-- Add the class icon to your logo image or logo icon to add the margining -->
                PregunteMe
            </a>
            <!-- Header Navbar: style can be found in header.less -->
            <nav class="navbar navbar-static-top" role="navigation">
                <!-- Sidebar toggle button-->
                <a href="#" class="navbar-btn sidebar-toggle" data-toggle="offcanvas" role="button">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </a>
                <div class="navbar-right">
                    <ul class="nav navbar-nav">
		                {% if app.user is null %}
			                <li class="dropdown user user-menu">
			                	<a href="{{path("login")}}">Ingresar</a>
							</li>
						{% else %}
		                    <!-- User Account: style can be found in dropdown.less -->
		                    <li class="dropdown user user-menu">
		                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
		                            <i class="glyphicon glyphicon-user"></i>
		                            <span>{{app.user.username}}<i class="caret"></i></span>
		                        </a>
		                        <ul class="dropdown-menu">
		                            <!-- User image -->
		                            <li class="user-header bg-light-blue">
		                                <img src="{{ asset('bundles/preguntemepresentacion/img/avatar5.png')}}" class="img-circle" alt="User Image" />
		                                <p>
			                                {{app.user.username}}<br>
		                                    <a href="{{path("logout")}}">Salir</a>
		                                </p>
		                            </li>

		                        </ul>
		                    </li>
						{% endif %}
                    </ul>
                </div>
            </nav>
        </header>
        <div class="wrapper row-offcanvas row-offcanvas-left">
            <!-- Left side column. contains the logo and sidebar -->
            <aside class="left-side sidebar-offcanvas">
                <!-- sidebar: style can be found in sidebar.less -->
                <section class="sidebar">
	                <ul class="sidebar-menu">
		                {% if not app.user is null %}
							Opciones Generales
									<li><a href="{{path("seleccion_modulo")}}"><i class="fa fa-tags"></i> Seleccionar Modulo</a></li>
						            <li><a href="{{path('inscripcion')}}"><i class="fa fa-file-o"></i> Crear nuevo proceso</a></li>
						            <li><a href="#"><i class="fa fa-bars"></i> Ver historial pedidos</a></li>
						            <li><a href="{{path('logout')}}"><i class="fa fa-times-circle"></i> Salir</a></li>
				            Administración 
									<li><a href="{{path('admin_usuario')}}"><i class="fa fa-users"></i> Adminsitrar usuarios</a></li>
							Contenidos en bloque
									<li><a href="{{path('cargar_archivo')}}"><i class="fa fa-cloud-upload"></i> Cargar archivo</a></li>
									<li><a href="{{path('descargar_archivo')}}"><i class="fa fa-cloud-download"></i> Descargar archivo</a></li>
							Crud en detalle
									<li><a href="{{path('admin_modulo')}}"><i class="fa fa-bar-chart-o"></i> Adminsitrar modulos</a></li>
									<li><a href="{{path('admin_dimension')}}"><i class="fa fa-bar-chart-o"></i> Adminsitrar dimensiones</a></li>
									<li><a href="{{path('admin_categoria')}}"><i class="fa fa-bar-chart-o"></i> Adminsitrar categorias</a></li>
									<li><a href="{{path('admin_indicador')}}"><i class="fa fa-bar-chart-o"></i> Adminsitrar indicadores</a></li>
									<li><a href="{{path('admin_nivel')}}"><i class="fa fa-bar-chart-o"></i> Adminsitrar niveles</a></li>
									<li><a href="{{path('admin_pregunta')}}"><i class="fa fa-bar-chart-o"></i> Adminsitrar preguntas</a></li>
									<li><a href="{{path('admin_respuesta')}}"><i class="fa fa-bar-chart-o"></i> Adminsitrar respuestas</a></li>
									
						{% else %}
							Opciones Generales
						            <li><a href="{{path('login')}}"><i class="fa fa-user"></i> Ingresar</a></li>
						            <li><a href="{{path('registro')}}"><i class="fa fa-users"></i> Registro</a></li>
						
						{% endif %}
                    </ul>
	            </section>
                <!-- /.sidebar -->
            </aside>

            <!-- Right side column. Contains the navbar and content of the page -->
            <aside class="right-side">
                <!-- Content Header (Page header) -->
                <section class="content-header">
                    <h1>
	                    {%if datos is defined %}

		                    {{datos.titulo}}
		                    <small>{{datos.subtitulo}}</small>
		                {% endif %}
                    </h1>
                    <!--ol class="breadcrumb">
                        <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
                        <li class="active">Blank page</li>
                    </ol-->
                </section>

                <!-- Main content -->
                <section class="content">
					{% for type, messages in app.session.flashbag.all() %}
						{% for message in messages %}
							<div class="alert alert-{{ type }} bg-{{ type }}">
								{{ message }}
							</div>
						{% endfor %}
					{% endfor %}
					{% block body %}{% endblock %}

                </section><!-- /.content -->
            </aside><!-- /.right-side -->
        </div><!-- ./wrapper -->
            	
        
        {% block javascripts %}{% endblock %}
        
        <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
        <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js" type="text/javascript"></script>
        <!-- AdminLTE App -->
        <script src="{{ asset('bundles/preguntemepresentacion/js/AdminLTE/app.js')}}" type="text/javascript"></script>

    </body>
</html>
