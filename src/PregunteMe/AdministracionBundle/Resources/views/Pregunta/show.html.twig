{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pregunta</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Texto</th>
                <td>{{ entity.texto }}</td>
            </tr>
            <tr>
                <th>Puntajemin</th>
                <td>{{ entity.puntajeMin }}</td>
            </tr>
            <tr>
                <th>Evaluador</th>
                <td>{{ entity.evaluador }}</td>
            </tr>
            <tr>
                <th>Cardinal</th>
                <td>{{ entity.cardinal }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_pregunta') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_pregunta_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
