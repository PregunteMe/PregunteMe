{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pregunta list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Texto</th>
                <th>Puntajemin</th>
                <th>Evaluador</th>
                <th>Cardinal</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_pregunta_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.texto }}</td>
                <td>{{ entity.puntajeMin }}</td>
                <td>{{ entity.evaluador }}</td>
                <td>{{ entity.cardinal }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_pregunta_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_pregunta_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_pregunta_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
