{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CasoEstudio list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Institucion</th>
                <th>Programa</th>
                <th>Nombre</th>
                <th>Correo</th>
                <th>Fecha</th>
                <th>Nombreobjeto</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_casoEstudio_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.institucion }}</td>
                <td>{{ entity.programa }}</td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.correo }}</td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.nombreObjeto }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_casoEstudio_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_casoEstudio_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_casoEstudio_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
